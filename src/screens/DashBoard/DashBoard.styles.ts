import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    backgroundColor: "#111315",
  },
  main: {
    flex: 1,
    padding: 24,
  },
  metrics: {
    flexDirection: "row",
    gap: 20,
    marginTop: 20,
  },
});
